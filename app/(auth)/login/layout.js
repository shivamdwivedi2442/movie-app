export const metadata = {
  title: "Log In",
  description: "Log in to your CINE HUB account to save favorites, track watch history, and pick up where you left off.",
  alternates: { canonical: "/login" },
  robots: { index: false, follow: true },
};

export default function LoginLayout({ children }) {
  return children;
}